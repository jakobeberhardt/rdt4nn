benchmark:
  name: Test
  description: A simple test benchmark which installs packages
  max_t: 40
  log_level: info
  scheduler:
    implementation: default  # Use default scheduler
    rdt: false  # Disable RDT for simplicity
  data:
    profilefrequency: 50  # Profile every 50ms
    db:
      host: ${INFLUXDB_HOST}
      name: ${INFLUXDB_BUCKET}
      user: ${INFLUXDB_USER}
      password: ${INFLUXDB_TOKEN}
      org: ${INFLUXDB_ORG}  
    rdt: false
    perf: true
    dockerstats: true

container0:
  index: 0
  image: nginx:alpine
  start: 0
  core: 0
  command: "sleep 20 && apk add firefox"
container1:
  index: 1
  image: nginx:alpine
  start: 0
  core: 1
  command: "sleep 20"
container2:
  index: 2
  image: nginx:alpine
  start: 0
  core: 2
  command: "apk add tar && tar -cvf dir.tar ~"
container3:
  index: 3
  image: nginx:alpine
  start: 0
  core: 3
  command: "while :; do dd if=/dev/zero of=/dev/null bs=1M count=100 2>/dev/null; done"
container4:
  index: 4
  image: nginx:alpine
  start: 0
  core: 4
  command: "while :; do dd if=/dev/zero of=/dev/null bs=1M count=100 2>/dev/null; done"
container5:
  index: 5
  image: nginx:alpine
  start: 0
  core: 5
  command: "while :; do dd if=/dev/zero of=/dev/null bs=1M count=100 2>/dev/null; done"
container6:
  index: 6
  image: nginx:alpine
  start: 0
  core: 6
  command: "while :; do dd if=/dev/zero of=/dev/null bs=1M count=100 2>/dev/null; done"

